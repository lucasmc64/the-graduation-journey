/*
#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Revisao do Basico\n\n");
    // ListaC01 - Programação Procedimental | Questão 46

    int number;
    int hundred, ten, unity, result;

    printf("Digite um numero de 100 a 999: ");
    scanf("%d", &number);

    hundred = number / 100;
    ten = (number % 100) / 10;
    unity = ((number % 100) % 10);

    result = unity * 100 + ten * 10 + hundred;

    printf("Numero lido: %d \nNumero gerado: %d", number, result);

    return 0;
}
*/
